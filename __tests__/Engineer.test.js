const Engineer = require('../lib/Engineer');

//create the engineer object
test('engineer object should be created', () => {
    const engineer = new Engineer('May', 223, 'may.engineew@jobname.net', 'github.com/may');
});
